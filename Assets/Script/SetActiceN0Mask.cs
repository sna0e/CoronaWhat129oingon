using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SetActiceN0Mask : MonoBehaviour
{
    public GameObject maskPlayer;
    public GameObject noMaskPlayer;
   
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    { 
        /*
        if (Input.GetKey(KeyCode.R))
        {
            if (setActive.maskState == false)
            {
                maskPlayer.SetActive(true);
                noMaskPlayer.SetActive(false);
                setActive.maskState = true;
                maskPlayer.transform.position = noMaskPlayer.transform.position;
                maskPlayer.transform.rotation = noMaskPlayer.transform.rotation;
            }
        }
        */
    }
}
